// URL prefix for reverse-proxy deployments under a path (e.g. /wlingo).
// Empty by default; see CLAUDE.md's "Root-path handling" for when to set it.
export const ROOT_PATH = import.meta.env.VITE_ROOT_PATH || ''
